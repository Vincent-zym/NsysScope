{
  "skill_name": "sglang-nsys-static-analysis",
  "evals": [
    {
      "id": 1,
      "prompt": "分析 GLM5.2 的 prefill nsys 报告。输入包括 .nsys-rep、SGLang 源码、config.json、启动 YAML、chunk-size=64 和 Nvidia B200。选择一个非 shared-indexer 的完整重复单元，中间不要停下来询问；在新任务目录中输出完整结果。",
      "expected_output": "Assistant reads model/design/config/launch evidence first, exports sqlite without overwriting useful data, selects a complete non-shared-indexer repeating unit, builds evidence-backed module/call-chain mappings, and emits exactly six normalized CSV tables plus manifests/stat sidecars in a newly created output directory. Shape/MFU cells are filled only when M/N/K, dtype and verified dense hardware peak are defensible.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "给定一个已生成的 layer_operator_timings.csv、模型源码和启动参数，生成标准化静态分析结果：原始表、算子总览、核心计算、辅助算子、三分类汇总和功能阶段汇总。功能模块不能直接照抄 module。",
      "expected_output": "Assistant creates a model-specific ordered semantic map, distinguishes broader Chinese 功能模块 from fine-grained module, runs build_static_analysis_tables.py, preserves full demangled names only in the origin table, simplifies names elsewhere, and validates exact filenames, headers, coverage, totals and descending sorts.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "分析一个双流 MoE trace，其中通信和专家计算重叠。输出六张表并给出各类耗时占比。",
      "expected_output": "Assistant preserves one-stream-per-module-occurrence grouping, uses the sqlite repeating-unit wall-span as denominator, sums per-category kernel durations without serializing overlapping streams, explicitly notes that aggregate percentages may exceed 100%, and emits all three classification rows.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "生成 B200 上某层的核心计算表，但提供的材料不能确定 GEMM 的 M 维，也没有注明 dense 或 sparse 理论峰值。",
      "expected_output": "Assistant completes best-effort analysis without inventing shape or MFU, leaves those cells blank, records the missing evidence in the manifest, and still generates all six required CSV files.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "分析一个四层结构周期：前三个结构单元走 KDA，第四个走 MLA，四层都带 MoE。生成标准七表和 NsysScope analysis.json。",
      "expected_output": "Assistant first writes and validates a current-model architecture taxonomy, records four ordered positions and both KDA/MLA variants with source discriminators, annotates every row with unit position/id/variant, aggregates by position plus variant plus functional module, preserves KDA and MLA as separate stage views, labels the primary duration as a heterogeneous structural-cycle duration, and rejects any output that collapses the two attention variants into a generic single-layer average.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "在 PP 中间 rank 的 prefill trace 上生成 forward 链路耗时表。该 rank 没有 embedding kernel，但有一个每个 KDA/KDA/KDA/MLA 结构周期发射一次的 kernel，它的发射间隔比真正每 forward 一次的 kernel 更均匀。",
      "expected_output": "Assistant refuses the per-cycle kernel as the step marker: it divides the device's total variant-core count by the taxonomy's unit layer count and notices that the resulting cycle count equals that candidate's launch count, then re-picks a marker with that launch count excluded. The published step period covers a whole forward with every repeating unit inside it, the total row states that the layer rows only cover this rank's layers, and the manifest records marker_auto_selected with the reason for the re-pick.",
      "files": []
    },
    {
      "id": 7,
      "prompt": "一张 trace 只包含一次完整 forward，或者只能找到每周期一次的锚点，仍然要求输出 forward 链路耗时表。",
      "expected_output": "Assistant does not publish a table built on an unverifiable boundary: it states that at least two complete forward steps are required, records forward_pipeline.skipped_reason in the manifest, keeps the six mandatory tables intact, and omits the table rather than presenting a structural period as a forward step.",
      "files": []
    },
    {
      "id": 8,
      "prompt": "decode trace 带投机采样但没有 CUDA graph（eager 模式），给出 target 与 draft 的耗时拆分。",
      "expected_output": "Assistant falls back from graphId to the step marker plus layer segmentation: the target forward ends at its last layer before the draft starts, the draft forward starts at the marker's draft gridX population and ends one median layer stride after its last layer core. 备注 states that each forward's tail (lm_head, sampling loop) is counted in the following prep phase, and target + draft still closes against the step within tolerance.",
      "files": []
    },
    {
      "id": 9,
      "prompt": "线上服务抓的 prefill trace：每步 token 数不同，forward 周期在 349~593ms 之间抖动，step marker 的 gridX 有 2~3 种取值。给出 forward 链路耗时表。",
      "expected_output": "Assistant does not reject the capture for being non-uniform: it relaxes the marker uniformity threshold in tiers and accepts a launch count corroborated by >=3 independent per-forward kernels, recording cv_threshold/cv_relaxed in the manifest. It refuses to read the multiple gridX populations as speculative decoding because they do not repeat as a fixed K-draft-then-one-verify pattern, so the table has no draft phase and speculative_tokens is 0.",
      "files": []
    },
    {
      "id": 10,
      "prompt": "PP=8 的 trace，taxonomy 声明的重复单元是 3x(KDA,KDA,KDA,MLA)，但 kernel 数最多的那张卡实际是 8 KDA + 4 MLA。生成 forward 链路耗时表。",
      "expected_output": "Assistant recognises that each pipeline rank owns a different slice of the layer stack, so it ranks devices by how closely their variant mix matches the declared unit instead of taking the busiest one, and publishes the table for a rank that reproduces 9 KDA + 3 MLA. device_candidates/device_rejected record the walk, the shard note states which rank the layer rows cover, and no table is published against a mismatching rank.",
      "files": []
    },
    {
      "id": 11,
      "prompt": "供的源码 commit 与 trace 里 captured SGLANG_BUILD_COMMIT 不一致。给出七表分析。",
      "expected_output": "Assistant treats the mismatch as narrowly scoped: source defaults and branch assertions are marked unverified, but it still cites file:line call chains, quotes the dispatching statement in dispatch_code_snippet, writes Chinese function_introduction, and derives GEMM M/N/K plus MFU/MBU from config and captured token counts. It never replaces those columns with 'source commit unverified' prose; validate_analysis_package.py rejects such a package.",
      "files": []
    },
    {
      "id": 12,
      "prompt": "PP=8 的 prefill trace，只有第一次 forward 完整落在 capture 开头，rank 0 大部分时间在等 SendRecv。给出重复单元的单层算子表。",
      "expected_output": "Assistant refuses to build the unit from rank 0 or from a single occurrence: it picks a rank whose layer mix reproduces the declared unit, samples at least 3 steady-state occurrences (never the capture's first forward), aligns the window to the real layer-start kernel so positions of one variant hold the same operator count, and never attributes a 400ms pipeline SendRecv wait to a 102ms layer. single_sample_fallback and an operator longer than its unit are validation failures.",
      "files": []
    },
    {
      "id": 13,
      "prompt": "这份 capture 只抓到一个 forward step，其他证据齐全。请给出七表分析。",
      "expected_output": "Assistant states that the seventh table (<prefix>_forward_pipeline_table.csv) cannot be derived from a single forward step, that it is a required table rather than an optional extra, and asks for a capture with repeated steps instead of shipping six tables or inventing a step breakdown."
    }
  ]
}
